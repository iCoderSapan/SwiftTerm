// ExtensionsTerminal.swift
// Placeholder recreated to satisfy Xcode build file reference.
// Original file appears to have been removed upstream; no current code references this.
// If upstream restores functionality, replace this stub with actual implementation.

import Foundation

// Empty extension reserved for future backward compatibility.
// Add any legacy APIs that callers outside the fork might still expect here.
extension Terminal {
    // Example legacy shim (uncomment and implement if needed):
    // public func legacyNoOp() { /* intentionally empty */ }
}
